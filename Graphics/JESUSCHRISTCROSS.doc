#include<iostream.h>
#include<conio.h>
void main()
{
   clrscr();
   int a,b;
   for(a=1;a<=5;a++)
   {
     for(b=1;b<=80;b++)
     {
       cout<<" ";
     }
   }
   for(a=1;a<=80;a++)
   {
      if(a>=35 && a<=40)
      {
	 textbackground(YELLOW);
	 cprintf(" ");
      }
      else
      cout<<" ";
   }
   for(a=1;a<=5;a++)
   {
      for(b=1;b<=80;b++)
      {
	 if(b==35 || b==40)
	 {
	    textbackground(CYAN);
	    cprintf(" ");
	 }
	 else
	 cout<<" ";
      }
   }
   for(a=1;a<=80;a++)
   {
      if((a>=25 && a<=35)||(a>=40 && a<=50))
      {
	 textbackground(GREEN);
	 cprintf(" ");
      }
      else
      cout<<" ";
   }
   for(a=1;a<=5;a++)
   {
      for(b=1;b<=80;b++)
      {
	 if(b==25 || b==50)
	 {
	    textbackground(MAGENTA);
	    cprintf(" ");
	 }
	 else
	 cout<<" ";
      }
    }
   for(a=1;a<=80;a++)
   {
      if((a>=25 && a<=35)||(a>=40 && a<=50))
      {
	 textbackground(BLUE);
	 cprintf(" ");
      }
      else
      cout<<" ";
   }
   for(a=1;a<=10;a++)
   {
      for(b=1;b<=80;b++)
      {
	 if(b==35 || b==40)
	 {
	    textbackground(RED);
	    cprintf(" ");
	 }
	 else
	 cout<<" ";
      }
   }
   for(a=1;a<=80;a++)
   {
     if(a>=35 && a<=40)
     {
	 textbackground(WHITE);
	 cprintf(" ");
     }
     else
     cout<<" ";
   }
   getch();
}